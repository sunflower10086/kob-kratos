package ws

// Stubbed SendMsg for now as dependencies are missing
func (c *Client) SendMsg() {
	/*
		for {
			select {
			case data := <-snake.Space.Game:
				sendMap(data)
			case data := <-snake.Space.ServiceDirection:
				logger.SugarLogger.Debugf("for web step %v", data)
				sendDirection(data)
			case data := <-snake.Space.Result:
				fmt.Println(data)
				sendResult(data)
			}
		}
	*/
}
